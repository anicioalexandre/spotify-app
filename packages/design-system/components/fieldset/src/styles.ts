import { css } from 'lit-element'

export default css`
  .fieldset {
    border: none;
  }

  .fieldset legend {
    padding: 10px 0;
  }

  .label {
    color: var(--white);
    font-size: var(--l-font-size);
    font-family: var(--font-family);
  }
`
